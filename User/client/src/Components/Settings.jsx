import React from "react";

export default function Settings() {
  return (
    <section>
      <h2 className="text-xl font-semibold text-[var(--text-h)] mb-3">Settings</h2>
      <p className="text-[var(--text)] leading-relaxed">Application preferences and notifications.</p>
    </section>
  );
}
